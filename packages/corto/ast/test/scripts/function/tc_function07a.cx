bool result : true

void fail(string msg):
    "tc_function07a: FAIL: $msg"
    result = false

class Point:: x, y: int32

void func_nested(Point p):
    if p.x != 10: fail("p.x != 10")
    if p.y != 20: fail("p.y != 20")
    if "$p" != "{10,20}": fail("\"$p\" != \"{10,20}\"")

void func(Point p):
    if p.x != 10: fail("p.x != 10")
    if p.y != 20: fail("p.y != 20")
    if "$p" != "{10,20}": fail("\"$p\" != \"{10,20}\"")
    func_nested(p)

void func_null(Point p):
    if p: fail("p")

Point a: 10, 20
var Point b = a
var Point c = Point{10, 20}
var Point d = null

func(Point{10, 20})
func(a)
func(b)
func(c)

func_null(null)
func_null(d)

if result: "tc_function07a: OK"
