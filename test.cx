bool result : true

void fail(string msg):
    "tc_function15a: FAIL: $msg"
    result = false

void func(sequence{int32} a):
    if a[0, 1, 2] != (1, 2, 3): fail("a[0, 1, 2] != (1, 2, 3)")
    if "$a" != "{1,2,3}": fail("\"$a\" != \"{1,2,3}\"")

func({1, 2, 3})

if result: "tc_function15a: OK"
