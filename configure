export CORTEX_BUILD=`echo $( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )`/build
export CORTEX_HOME=$HOME/.cortex
export CORTEX_TARGET=$HOME/.cortex
export PATH=$CORTEX_TARGET/bin:$PATH
export CORTEX_VERSION=0.2.0-alpha
